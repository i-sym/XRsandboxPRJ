export interface HitPlacementProps {
    children?: never;
}

export function HitPlacement(props: HitPlacementProps) {
    return (
        <div className="hit-placement">
            <h1>Hit Placement</h1>
        </div>
    );
}